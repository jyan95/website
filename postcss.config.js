export default {
  plugins: {
    // Tailwind CSS plugin for PostCSS.
    // This tells PostCSS to run Tailwind's compilation process.
    '@tailwindcss/postcss': {},
    // Autoprefixer adds vendor prefixes to CSS rules for broader browser compatibility.
    autoprefixer: {},
  },
};
